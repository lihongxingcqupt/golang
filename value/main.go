package main

import (
	"fmt"
	"math"
)

var a int

func main1() {
	var res int
	res = 1
	name := "jack"
	fmt.Println(res)
	fmt.Println(name)
	minVal := math.Min(1, 2)
	fmt.Println(minVal)
	test()
	fmt.Println(a)
}
